name: Docker Build and Push

on:
  push:
    branches:
      - main
      - develop
      - Docker_file/check

  pull_request:
    branches:
      - main
      - develop
      - Docker_file/check

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
        - name: Checkout repository
          uses: actions/checkout@v4
      
        - name: Log in to Docker Hub
          uses: docker/login-action@v3
          with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
      
        - name: Build image
          run: |
            docker build -t ${{ secrets.DOCKER_USERNAME }}/Test_Image .
    
        - name: Push image
          run: |
            docker push ${{ secrets.DOCKER_USERNAME }}/Test_Image

 
